

The application uses Python on the server side and runs on Google App Engine.
The python script is a morphological analyser/parser which upon receiving an input in the form of single Kabyle word 
will return the following results:
a. Roots: the root of the word.
b. Plural: This returns the plural form of a noun
